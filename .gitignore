# Dependencies
node_modules/

# Environment secrets
.env

# OS files
.DS_Store
Thumbs.db

# Generated uploads (keep directory structure, ignore runtime files)
public/uploads/thumbnails/*
public/uploads/references/*
!public/uploads/thumbnails/.gitkeep
!public/uploads/references/.gitkeep

# Unified runtime database (users, credits, transactions, generations — NEVER commit)
database.json

# User data directories (runtime generated images, references, prompts — NEVER commit)
user_data_image_generate/
user_data_image_reference/
user_data_prompt/

# Admin data directories (runtime generated images, references, prompts — NEVER commit)
admin_data_image_generate/
admin_data_image_reference/
admin_data_prompt/

# IDE
.vscode/
.idea/
